[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build && composer install --optimize-autoloader --no-interaction"

[deploy]
startCommand = "bash scripts/start.sh"
healthcheckPath = "/up"
healthcheckTimeout = 600
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
